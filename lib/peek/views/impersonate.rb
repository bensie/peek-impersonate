module Peek
  module Views
    class Impersonate < View
      # A view to show the currently signed in master, the currently
      # signed in user, and the ability to switch the currently logged
      # in user by providing their email address.
      #
      # Returns Peek::Views::Impersonate
      def initialize(options = {})
      end
    end
  end
end
